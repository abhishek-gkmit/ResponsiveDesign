import { colors } from './colors';

const { light, dark } = colors;

export const GRADIENTS = {
  light: {
    loginScreen: [light.mauve, light.tropicalIndigo, light.primary],
  },
  dark: {
    loginScreen: [dark.mauve, dark.tropicalIndigo, dark.primary],
  },
};
